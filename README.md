# Blog

Simple Express + Simple EJS blog project.

How to run:

```powershell
npm install
node index.js
# or
npm run start
```

To add a remote repository:

```powershell
git remote add origin <REMOTE_URL>
git push -u origin main
```
